# 语料清洗问题汇总备忘

本文件记录《全唐诗》HTML 语料清洗中的**已知问题、处理策略与口径说明**。

---

## 常用命令

| 命令 | 作用 |
|------|------|
| `python tools/scan_merged_author_quotation.py` | 作者行与诗句首句粘连 |
| `python tools/scan_missing_author.py` | 无作者分类（体例 / 疑似漏标 / 继承统计） |
| `python tools/validate_pian_ti_sequence.py` | 篇题序号跳号、重复 |
| `python tools/scan_corpus_structure.py` | 篇题错位、句编卷清单 |
| `python tools/scan_corpus_structure.py --fix-safe` | 自动修复明确篇题错位 |
| `python tools/scan_unclosed_bracket_titles.py` | 篇题 `【` 未闭合；`--fix-safe` 补短题名 `】` |
| `python tools/diagnose_meili_ticai.py` | 对比 JSONL / PostgreSQL / Meilisearch（可选） |
| `python tools/rebuild_all.py` | 一键：索引 + PG + Meili `--recreate` |
| `python tools/rebuild_all.py --diagnose` | 同上，结束后跑诊断 |

**改 HTML 或索引逻辑后：**

```bash
python tools/rebuild_all.py
```

`build_phase1_index.py` 已含**卷内作者继承**；Meilisearch 须 `--recreate` 以免旧 id 残留。

---

## 一、作者与诗句粘连（已处理）

**现象**：`<p class="Quotation">` 含作者名 + 诗句首句。

**已修 6 处**：text00024、text00021（×3）、text00427、text00607（句编条）。

---

## 二、篇题序号断档（部分已处理）

**现象**：`卷164_12` 后直接 `卷164_33` 等。

**已修**：text00164（_33–_45 改为 _13–_25；《秦女休行》改 Bold）。

**待查**：`index/pian_ti_sequence_report.json`（约 61 卷仍有跳号，需对照目录逐卷改，勿盲目重排）。

---

## 三、篇题错位（已自动修复 23 处）

**现象**：

- 篇题写在 `Text` 行（应为 `Bold`）
- 篇题写在 `Quotation`，作者写在下一行 `Bold`
- `Quotation` 粘连「篇题 + 作者」（如 `卷237_96【…】钱起`）

**批量修复**：`scan_corpus_structure.py --fix-safe` 共 **23 处**，涉及 19 个文件。

**当前扫描**：篇题错位 **0 处**（`corpus_structure_report.json`）。

---

## 四、篇题【】未闭合（已全库处理）

**现象**：篇题 `Bold` 仅有 `卷1_3【执契静三边`，缺 `】`，列表显示带卷号前缀的残题。

**类型**：

| 类型 | 处理 |
|------|------|
| 短题名缺 `】`（29 处） | `--fix-safe` 已补全 |
| `】` 错在下一行 `Text` 末（2 处） | 已移至篇题并去掉正文尾 `】` |
| 篇题拆成两个 `Bold`，`】` 在第二行（1 处） | 已合并为一行 `Bold` |

**当前**：`scan_unclosed_bracket_titles.py` 扫描 **0 处**（`index/unclosed_bracket_report.json`）。

**前端**：`extractTitle` 在仅有 `【` 无 `】` 时也会尽量只显示篇名（修 HTML 后仍以底本为准）。

---

## 五、句编 / 摘录卷（体例特殊，非一律错误）

**识别规则**（`scan_corpus_structure.py`）：

- `PoetryText` 行占比高（>35% 且 >5 行），或
- 卷首 `Bold` 仅为「句」，或
- 篇题含 `【句】`

**约 160 卷** 列入 `jubian_volumes`，见 `index/corpus_structure_report.json`。

**处理原则**：

- 不强行改成 `Bold` + `Quotation` + `Text` 诗体
- `parse_note: orphan_prose_block` 可能较多，属预期
- 名句汇编条允许多作者、多行并列

---

## 六、无作者（策略 + 当前数据）

### 6.1 三类情况（不要混为一谈）

| 类型 | 含义 | 处理 |
|------|------|------|
| **体例性无作者** | 郊庙歌辞、部分乐府等原书无逐首 `Quotation` | 保留空 `zuo_zhe`；检索用体裁/篇题 |
| **解析性假无作者** | HTML 有作者但 `Quotation` 带 `style` 等，曾未被解析 | 已修：`TAG_RE` 允许标签属性 |
| **疑似漏标** | 诗人别集：同卷其它首有作者，本篇无 `Quotation` | **补 HTML** `<p class="Quotation">` |

### 6.2 卷内作者继承（已实现）

- 按 `source_file` 顺序向前填充空 `zuo_zhe`（歌辞/句编等卷不继承）。
- 标注 `parse_note: author_inferred_from_volume`（**不**算结构错误，检索仍包含）。
- 当前约 **202 条** 靠继承获得作者。

### 6.3 扫描报告

路径：`index/missing_author_report.json`

| 分类 | 约条数 | 说明 |
|------|--------|------|
| 仍无作者 | ~1240（3%） | `zuo_zhe` 为空 |
| `corpus_geci` | ~380 | 郊庙/乐章等，体例正常 |
| `orphan_prose` | ~471 | 句编/杂录 |
| `suspected_missing_markup` | ~340 | **优先人工补** `Quotation` |
| `inferred_from_volume` | ~202 | 已继承（`zuo_zhe` 非空） |

### 6.4 已修：带 `style` 的 Quotation

```html
<p class="Quotation" style="text-align: center;">张籍</p>
```

### 6.5 不建议

- 为歌辞、郊庙虚构作者；外源考证宜放第二期 enrichment，不直接改 HTML。
- 对句编卷强行拆成单作者 `Quotation`。

---

## 七、体裁：两套口径（重要）

前端「体裁大类」**不是**教材里的绝句 / 律诗 / 古风分类，而是**从篇题第一个 `【】` 解析出的《全唐诗》原书目录名**。

### 7.1 本库怎么解析

| 篇题示例 | `ticai_da`（大类） | `ticai_xiao`（小类） |
|----------|-------------------|---------------------|
| `【杂曲歌辞·辽东行】` | 杂曲歌辞 | 辽东行 |
| `【郊庙歌辞·祀圜丘乐章·豫和】` | 郊庙歌辞 | 祀圜丘乐章·豫和 |
| `【逢雪宿芙蓉山主人】` | （空） | 逢雪宿芙蓉山主人 |

规则：【】内若有 `·`，点前为大类、点后为小类；无 `·` 则仅小类（多为诗题），大类为空。

### 7.2 与「绝句、律诗、乐府」的关系

| 维度 | 例子 | 回答的问题 |
|------|------|------------|
| **本库 `ticai_da`** | 杂曲歌辞、相和歌辞、郊庙歌辞 | 在全书里**挂在哪类歌辞/乐章下** |
| **格律诗体**（教学常用） | 五绝、七绝、五律、七律、古风 | **几句、是否近体、格律** |

二者可并存，**不能互相替代**。诗人别集里的名篇（如刘长卿《逢雪宿芙蓉山主人》）篇题通常**只有诗名**，筛「杂曲歌辞」**筛不到**，不是漏收。

### 7.3 算不算「权威」

- **对《全唐诗》篇题编次**：忠实拆 `【】`，可回溯 HTML，数字人文上稳妥。
- **对当代绝句/律诗分类**：**不算、也未实现**；需 Phase 2 按句数/平仄等**另算派生字段**（见 `docs/07`）。

学界常并存多轴分类：

- 《全唐诗》/《乐府诗集》体系的**歌辞类名**（本库现状）；
- 格律学的**绝/律/古**；
- **题材**（山水、边塞）又是另一轴。

### 7.4 前端筛选与计数

- **检索「共 N 条」**：Meilisearch（关键词 + 作者/卷/目录 + 是否排除解析异常）。
- **下拉旁数字**（如杂曲歌辞 453）：PostgreSQL 统计，过滤条件与检索一致（排除解析异常条）；**不用** Meili facet（默认只显示 100 个且按字母序，会漏掉杂曲歌辞等大类）。
- 列表上方显示当前筛选摘要，避免把窄检索误当成库总量。
- **诗体**（绝句/律诗）与 **目录大类**（歌辞类）分开展示；诗体留待下一阶段。

---

## 八、口径与范围（备忘）

### 8.1 条数与「诗首」

- Phase 1 一条 = 一个篇题块（组诗「十首」可仍是一条）；与通行 ~4.89 **万诗首** 口径不同。
- **这不是错误**，而是统计单位不同；**现阶段不投入**统一条数、拆诗首、对外报诗首数等工作（见 `docs/07` 路线图，需要时再开 Phase 2a）。

### 8.2 苏州子集

- **下一阶段**再做（地理词表、`suzhou_phase1.jsonl`、专题检索等）；**当前阶段不考虑**。

### 8.3 其它清洗项（简述）

| 项 | 状态 | 说明 |
|----|------|------|
| 错别字 / 异文 | 另立审校项目 | 本库以结构清洗与检索为主，不自动改字 |
| 漏句 | 人工 / 他本 | 需高置信再改 HTML |
| 拆诗（诗首展开） | 暂缓 | 见 7.1 |
| 外版对照 | 未纳入本库 | 另文件夹保留作他用，不参与当前流水线 |

---

## 九、报告文件索引

| 文件 | 内容 |
|------|------|
| `index/pian_ti_sequence_report.json` | 篇题序号连续性 |
| `index/corpus_structure_report.json` | 篇题错位 + 句编卷 |
| `index/missing_author_report.json` | 无作者分类、疑似漏标 |
| `index/unclosed_bracket_report.json` | 篇题【】未闭合（当前应为 0） |

更完整设计见 `docs/02-术语与统计口径.md`、`docs/07-索引分期设计.md`。
