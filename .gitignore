# ── 密钥 / 本地环境 ──
.env
.env.*
!.env.example
!deploy/.env.prod.example
deploy/.env.prod

# ── Python / Node 构建产物 ──
__pycache__/
*.py[cod]
.venv/
venv/
node_modules/
apps/web/dist/

# ── 索引（服务器 pull 后由 build_phase1_index.py 生成）──
index/*.sqlite
index/*.jsonl
index/*_report.json
!index/.gitkeep

# ── 打包与系统垃圾 ──
dist-quatangshi-deploy.zip
.DS_Store
Thumbs.db
*.log

# ── IDE / Agent（仅本机 Cursor，生产不需要）──
.cursor/
AGENTS.md

# ── 个人备忘 ──
问题汇总备忘.md

# ── 废弃原型（正式前端为 apps/web）──
tang-verse-explorer/

# ── 设计文档（生产运行不需要；在 GitHub 网页或开发机查看）──
docs/

# ── 本地开发栈（生产用 deploy/docker-compose.prod.yml）──
docker-compose.yml
requirements-dev.txt

# ── 部署：仅本机 Windows 打包 / 未采用的宿主机 Nginx 方案 ──
deploy/pack-for-server.ps1
deploy/nginx-host-tp.conf

# ── 语料校对工具（仅开发机改 HTML 时用；生产 git pull 不需要）──
tools/convert_toc_to_juanmu.py
tools/normalize_juan_titles.py
tools/normalize_pian_ti_prefix.py
tools/validate_pian_ti_sequence.py
tools/scan_corpus_structure.py
tools/scan_merged_author_quotation.py
tools/scan_missing_author.py
tools/scan_unclosed_bracket_titles.py
tools/diagnose_meili_ticai.py
tools/query_meilisearch.py
tools/rebuild_all.py
